# Default Configurations
This folder contains the default config files that will be placed in the appropriate directories when the application is installed.
And these files can be customized by the user after installation (thru the config UI).