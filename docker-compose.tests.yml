services:
  go-integration-tests:
    build:
      context: ./desktop/apps/neuro-integration
      dockerfile: Dockerfile.test

  python-parser-tests:
    image: python:3.12-slim
    working_dir: /workspace
    volumes:
      - ./:/workspace
    command: python -m unittest discover desktop/backend/python/tests
