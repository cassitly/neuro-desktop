services:
  go-integration-tests:
    build:
      context: ./desktop/apps/neuro-integration
      dockerfile: Dockerfile.test
    command: go test ./...

  python-parser-tests:
    build:
      context: ./desktop/backend/python
      dockerfile: Dockerfile.test
